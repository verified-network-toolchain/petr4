name: ci-typechecker

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  #  push:
  #    branches: [ main ]
  #  pull_request:
  #    branches: [ main ]
  workflow_dispatch:

jobs:
  use-build-petr4:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: reuse build-petr4
        uses: ./.github/actions/build-petr4
    
#   build-p4c:
#     runs-on: ubuntu-latest
#     container:
#       image: p4lang/p4c
#     steps: 
#       - name: run p4c 
#         run: p4c --help
#       - uses: actions/checkout@main
#       - name: run petr4
#         uses: ./.github/workflow/build-petr4.yml


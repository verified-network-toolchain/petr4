# Transformations on `P4cub` Syntax

### Modules:

- `Unroll.v`: Loop-unrolling for `p4cub` parsers.

- `Lifting`: Lifts expressions to statement-level
  whose `clight` equivalents must be represented as
  function calls.